# Agent contract

See [CLAUDE.md](CLAUDE.md). It is the single agent contract for this repo and
applies to every harness, not just Claude Code.

Start at its **Role gate**: it decides whether you are Lelouch (the orchestrator)
or a dispatched Worker, and the two roles have opposite rules about writing
project code.

This file is a pointer, not a copy — a second copy would drift from the first.
